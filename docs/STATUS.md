# Project status and handoff

Last updated: 2026-09-26 (UTC; lead reconciliation). Read this after `AGENTS.md` when picking the project up. It records the current state, open decisions and known problems that are not obvious from the code. Update it when any of these change.

## Current focus (2026-09-26): verifying leads

Session focus after the 2026-09-26 compaction. Sixty-five **Unverified leads** remain: 8 from the single-transit pool, 57 from the `colab-p01` batch. State and queue:

- **Dossier front-runners (vetting rerun done, reviewer notes written):** TOI-2666.01 (survives every test; possible secondary alias at P 36.53 d fits the EB reading; box-fit χ²ν 21.8 noted) and TOI-3500.02 (E1 survives; E2 suspect; 9 aliases at P = 350.3/n d). Next: dossier decision — the site's `candidate` mechanism requires `publish/candidates/CYG-….json` records.
- **Batch vetting queue** (`python -m cygnus.campaign vet campaigns/<id>.yaml`, log per the `tess-mono-01` pattern): (1) guard collisions toi-6806-01, toi-630-01; (2) strongest alias structure toi-4381-01 (22), toi-7610-01 (21), toi-1229-01 (18), toi-224-01 (18), toi-1192-01 (14), toi-588-01 (13), toi-5149-01 (7); (3) pilot pendings toi-225-01 (40), toi-7857-01 (34), toi-6667-01 (29), toi-6695-01, toi-1835-02, toi-7399-01; (4) census/blend/injection groups. Machine inventory: scratch `site_collections/leads_inventory.json`; per-lead one-liners in the 2026-09-26 session summary.
- **Sharpening pass (optional first):** the committed records predate `fetch_independent` (ZTF + Kepler/K2 now wired); a recompute pass would run the independent-epoch checks on every lead. Code fingerprint changes make this a full recompute — batch it.
- **In flight at compaction:** the Gaia-cone sweep for the explorer (569/1,090 cones; 4 per-target retries pending; the pass is fault-tolerant and resumable — rerun `python design-system/mockups/fetch_sky_data.py --missing` until zero failures, then rebuild explorer + bundle and redeploy).

## Where things are

| Area | State | Docs |
|---|---|---|
| Science | One known-planet recovery and one residual screen (WASP-12, TESS S20/S43): **bounded null, no candidate**; at k = 5 the residual screen excludes only dips ≳ 1.5% deep. **`tess-mono-01` full declared pool completed 2026-09-25:** `target_queue.top` raised 20 → 100 and rebuilt to 76 members; all 76 targets are now run, reviewed and committed through the known-object loop (`docs/AGENT_RUNBOOK.md`), with ranks 37–76 handled in five batches of eight. Positive-control states: 32 passed, 22 failed, 12 inconclusive, 10 `not_tested`; failures are pipeline outcomes, not evidence against catalogued signals when their depths are below the measured completeness floors. Thirteen reports carry runner-flagged repeat events as **unverified leads**: TOI-2666.01, TOI-3500.02, TOI-5812.02, TOI-2318.01, TOI-6674.01, TOI-2065.01, TOI-6666.01, TOI-6695.01, TOI-225.01, TOI-1835.02, TOI-6667.01, TOI-7857.01 and TOI-7399.01. The reviews flag substantial shape/depth and artifact concerns for TOI-7857.01 and TOI-7399.01; neither has an established period. TOI-6650.04 also has an unresolved ~20.33-d, ~1% event train, escalated for multi-TOI / eclipsing-binary / blend checks; the runner did not assign it lead status. Earlier reviewer flags remain for TOI-790.03, TOI-2534.01 and TOI-3724.01. No candidate dossiers exist. **Lead vetting started and paused 2026-09-25** (`campaigns/tess-mono-01/LEAD_VETTING_LOG.md`; ledger runs #537–#545, #546–#547 aborted by the pause):
<br>• **Rejected:** TOI-5812.02 (a transit of the sibling WASP-134 b), TOI-2318.01 and TOI-6674.01 (systematics), TOI-2065.01 (scattered light), TOI-6666.01 (pointing common mode).
<br>• **Survived every test run:** TOI-2666.01; TOI-3500.02 event E1, whose events are spaced 700.62 d and 350.32 d apart.
<br>• **Not yet vetted:** six leads and three reviewer flags. **Resolved:** the ~20.33-d train of TOI-6650.04 is its sibling TOI-6650.01 (all six events within 0.5 h of a predicted transit). | `campaigns/`, `reports/`, their `sky_record.json` |
| Tier-1 baseline pack | 132 manifest rows from 8 archives, checksummed. Products live in private cloud storage (`cygnus:Cygnus/…`); the repo holds manifests only. Engineering baseline, not a scientific result. | `docs/tier1_pack/` |
| Public site | **Converted 2026-09-25 (user decision):** https://cygnus-sky.pages.dev serves the sky explorer at `/` (94 targets, including all 76 tess-mono-01 known-object tests with Gaia fields and survey images). The v1 generator's pages keep their paths, with its home page at `/about/`. `/preview/*` redirects to `/`. Leak scan clean; deployed by hand (steps in `docs/PUBLISHING.md`; latest deployment `e9050b8e`, 2026-09-26), not connected to git. **Redeployed 2026-09-26** (deployment `e9050b8e`, by hand with `npx wrangler@4 pages deploy build/pages --project-name cygnus-sky --branch main`). The bundle was rebuilt and leak-scanned (270 files, 18.1 MB) after the record regeneration, so the live `/data/sky.json` carries the corrected J2015.5 position epochs (checked on the live site: 0 `J2000.0 (TIC)` labels). No publish collection was added or changed; the NSS campaigns stay unpublished (open decision 3). Still v1-styled: the repository pages (`/campaigns/`, `/log/`, …). There are no per-target pages or observing log in the explorer design yet. | `docs/PUBLISHING.md`, `design-system/mockups/README.md` |
| Sky records | Every analysis must leave `sky_record.json`; tests fail otherwise. | `docs/SKY_RECORDS.md` |
| Design system "Ad Astra" | Source in `design-system/project/`. A browsable copy exists as a private claude.ai artifact (only reachable from Claude). It still describes the **v1** look, not the explorer. | `design-system/project/README.md` |
| Pipeline autonomy | Campaigns run from YAML specs through a ledgered, resumable runner with calibration (sign-flip null, injection–recovery), catalogue cross-match and a ranked target queue; CI and a weekly public-archive queue job exist. Known-object tests are generated and run by any agent with five commands (`docs/AGENT_RUNBOOK.md`), with a positive control, grouped screen events and period aliases. **Lead vetting** (`python -m cygnus.campaign vet`, 2026-09-25) adds per event:
<br>• sibling-TOI ephemerides, box-fit shape against the reference transit, detrending alternatives and red-noise significance;
<br>• background, centroid and pointing shifts, quality flags, and a TPF difference-image centroid;
<br>• Gaia neighbours, same-CCD common mode, and stellar-density and secondary-eclipse limits on the aliases.

Since 2026-09-26 generated specs also run `stellar_context`, `event_census`, `moving_objects`, `variability_guard`, `fetch_independent` (Kepler/K2 long-cadence products from MAST by position and ZTF light curves from IRSA, so the two independent checks actually run; later the same day, uncommitted), `alias_cross_instrument` (and `rv_bounds` when ESO is declared), and `period_aliases` carries a per-alias duration likelihood from Gaia-derived stellar density (`docs/SUITE_EXPANSION.md` §7.3). Not yet built: ADS, and a full period posterior (the likelihood assumes circular orbits and b ~ U(0,1)). | `docs/CAMPAIGNS.md`, `docs/AUTONOMY_STUDY.md` |
| Test suite (expanded 2026-09-26) | **Implemented** (user instruction 2026-09-26). Four lanes: offline gate `python -m pytest -q` (**590 passed, 37 deselected**, ~93 s); L1 seeded calibration `-o addopts= -m slow` (7); L2 checksum-pinned replay `-o addopts= -m replay` (4 committed campaigns, never downloads); L3 live known-answer probes `-o addopts= -m network` and `python -m cygnus.multi archives --check all` (24 probes; last run 19 ok, 5 unavailable, 0 error). The vet orchestrator, every multi step, every adapter and the runner/batch branch matrices now have tests; 15 bugs they found are fixed. Ten new record checks from previously unused adapter data (stellar priors, PM-propagated identification, blend census, per-event quality census, moving objects, VSX and SIMBAD guards, independent repetition, ZTF epochs, archival-RV bounds) plus a per-alias duration likelihood and an error-weighted box fit in vet. Disposition per item, calibration numbers and deferred items in the suite doc. | `docs/SUITE_EXPANSION.md` §7 |
| Repository | GitHub `mapsugui/ad_astra`, branch `main`. Licence Apache-2.0; third-party data keep their own terms (see README). | `README.md` |
| Multi-archive pipeline | **Merged into production 2026-09-25** as `src/cygnus/multi/` (documented in `docs/CAMPAIGNS.md`, *"Multi-archive campaigns"*); the former safe copy `experimental/` was **retired 2026-09-26** (see Known problems). Specs declare `runner: cygnus.multi` and `python -m cygnus.campaign run|check|report|vet` hands them over whole; the production runner refuses them elsewhere. 23 archive adapters (`cygnus.multi.archives.base.summary()`) cover `DATA_SOURCES.md` sections 1–3,5 plus Exoplanet Archive; product discovery/fetch/read is generalised through `archives/base.py` and `readers.py`, products carry kinds (`lightcurve`/`image`/`table`/`text`) so only time series are screened, and `fetch_products` can discover from any named archive. Per-source checks run in a `source_checks` step (Gaia RUWE/NSS flags, MAST quality census, MPC observations via `data.minorplanetcenter.net` — the main MPC site is still unreachable, image/table integrity, etc.); single-channel archive products screen once with `channel_mode: single` and a red-noise systematics model (`systematics.py`) instead of a false SAP-vs-PDCSAP comparison; `astrometric_vetting` cross-matches targets against Gaia DR3 NSS two-body solutions and reports the mass function where available. Runs are recorded under the `cygnus_multi:` ledger namespace; `CYGNUS_MULTI_ROOT` confines specs, outputs and ledger to a sandbox root (any directory); `python -m cygnus.multi.promote --root <sandbox>` (or with `CYGNUS_MULTI_ROOT` set; a root is required) copies a vetted sandbox campaign into `campaigns/` with `PROMOTED.md` provenance and a **draft** collection, never deploying. **Two campaigns run through the production path, both reviewed 2026-09-25:** `campaigns/toi-6666-01-nss/` — Gaia DR3 NSS cross-match of the TOI-6666.01 host (source 2223770483452673408, 0.019″): **no two-body solution** (`inconclusive`); it records that an earlier ad-hoc SB2 attribution was **withdrawn** (that solution belongs to source 4513527912472807936, 57.276° away — re-verified arithmetically). `campaigns/toi-2666-01-nss/` — same test for the TOI-2666.01 host (SIMBAD: HD 80133; source 3837451574150437120, 0.03″): **no two-body solution**; host RUWE 1.464 is recorded inconclusive — weakly consistent with an unseen companion, not evidence for one. No publish collections exist for either (open decision 3). **Supersede pass 2026-09-25: all 76 tess-mono-01 known-object campaigns were re-run through `cygnus.multi run --force` (fresh `cygnus_multi:` ledger runs for every step); equivalence against the production-runner results was verified target by target — screen entries, depths, k* calibration, injection–recovery and aliases match leaf-for-leaf, and the multi runner adds only annotations (product kind, screen-suitability census, red-noise systematics model). Two transient network failures (`toi-2493-01` cross-match, `toi-6666-01` MAST 502) were re-run individually until clean; each REPORT.md carries a supersede-provenance note.** Gate (2026-09-26, after `experimental/` was retired): **254 passed, 2 deselected**, a single suite with no separate sandbox tests. | `docs/CAMPAIGNS.md` |

## Open decisions (ask the user; do not assume)

1. **Site, next steps** (the conversion itself was approved and done 2026-09-25): per-target pages and an observing log in the explorer design; restyle the repository pages; update `design-system/project/` to match.
2. **Publish the restricted source archive** in `publish/collections/cygnus-software-0-1-0.json`? It was withheld while no licence existed; the licence is now Apache-2.0, but nobody has decided to publish it.
3. **TOI-2666.01 lead.** Pursue the Sector 99 repeat (ExoFOP/SPOC DV check, difference-image centroids, alias follow-up)? Nothing is submitted anywhere without the user's authorisation. The NSS vetting (`campaigns/toi-2666-01-nss/`) adds that Gaia DR3 has **no** NSS two-body solution for the host HD 80133 (`inconclusive`, not a clean pass; host RUWE 1.464 is weakly consistent with an unseen companion). No publish collection exists for either NSS campaign (`toi-2666-01-nss`, `toi-6666-01-nss`) — decide whether to create one for each and, separately, whether to publish (and whether to fold each result into its parent record, `toi-2666-01` / `toi-6666-01`).
4. **Logo.** Direction chosen and approved (2026-09-24): concept **D**, the Northern Cross badged with a transit chord at a seeded random angle (`design-system/brand/build_brand.py`, `transit_params`). Revised the same day to rigid four-point spike stars and a transit chord drawn as a double-ended blade that breaks through the rim, angle drawn from hard diagonals (25–65° or 115–155°). Still to pick: the seed (alternatives in `design-system/brand/index.html`). Then: final outlined SVGs, favicon set, explorer header, site and design system.
5. If the explorer's Gaia-derived files are published, Gaia's share-alike terms (CC BY-SA 3.0 IGO) apply to those files.
6. **Recompute the fleet with the new steps?** The 2026-09-26 suite expansion changed the multi code fingerprint, so every committed campaign would recompute on its next `run`, and generated specs now include six new steps. The 78 committed records were **not** regenerated. Options: re-run all 78 through `cygnus.batch` (hours; new checks appear in every record and on the site after a rebuild), re-run only the leads, or leave them until each is next touched. The same pass would move two past difference-image readings (TOI-2065.01, TOI-2318.01) from `passed` to `inconclusive` under the corrected rule (erratum in the lead vetting log; both leads stay rejected).
7. **~~Publish collection for the periodic-TOI batch?~~ Resolved 2026-09-26 (user: campaign pages for all campaigns).** Two collections now exist: `publish/collections/tess-periodic-01.json` (the 100 completed `colab-p01` campaigns) and `publish/collections/tess-single-transit-01.json` (the 78 known-object campaigns), each campaign with spec + report + search-log items; every outcome/evidence string is copied verbatim from the records. The 57 leads are deliberately **not** published as candidate dossiers yet (open decision 8: vetting first). The 900 queued-but-unrun periodic targets stay unpublished until they run. Run-level rows for the Colab batch are absent from `/log/` by design: the companion ledger is never merged and the site log mechanism keys on exact script names.
8. **Vet the 57 new leads?** `python -m cygnus.campaign vet` per `docs/AGENT_RUNBOOK.md`, logged in `campaigns/tess-periodic-01/` (the `LEAD_VETTING_LOG.md` pattern of `tess-mono-01`). Review-first order: the two catalogue-collision leads (`toi-6806-01` VSX KELT EA P=23.249 d, `toi-630-01` SIMBAD EB* at 0.1″), then the 21 leads with surviving alias periods, then the 31 event-census-flagged and 13 blend-census-inconclusive and 8 injection–recovery-inconclusive leads. Every lead is an **Unverified lead** until vetting runs.

## Known problems (surface them; do not paper over)

- Ledger run #1 measurements cite product `tesscut-219.7570--80.5310-sec12`, but the ledger's product row is `…-sec12-cam3-ccd1`. The site shows it as unresolved.
- Ten `cygnus.ingest.tier1` runs left `open` by crashed processes were closed as `aborted` on 2026-09-24 with a note; new runs close themselves.
- The ledger (`state/ledger.sqlite`) is not in git and was being written by ingest jobs during the last session; ledger-derived pages are only as fresh as the last build on a machine that has it.
- `docs/TEST_ARCHITECTURE_PLAN_DRAFT.md` lists unaddressed safety gaps: MAST live-test scratch isolation, pack retirement verifying zero rows, ledger referential integrity.
- The residual screen is now calibrated (see its report addendum); pixel-level, centroid, independent-reduction and ADS literature checks remain not tested.
- ~~The SIMBAD prior-art adapter failed with a proxy `Tunnel connection failed` ...~~ **Resolved 2026-09-25 (audit fix).** A live re-check returned all four services (`NASA Exoplanet Archive`, `TESS_TOI`, `VSX`, `SIMBAD`), and the ten records left `inconclusive` (nine with 3 answered/1 errored, `toi-6667-01` with 2/2) were re-run: every one now reports **4 answered, 0 errored** and check state `passed`. The proxy remains intermittent — any future cross-match left `inconclusive` by an errored service must still be re-run before it is treated as complete.
- The full `python -m pytest -q` gate passed after each completed target batch on 2026-09-25; as of the 2026-09-25 audit fixes it is **188 passed, 2 deselected** (the earlier 159 did not include the no-data, step-order, register-matching and restricted-manifest tests), and after the multi-archive runner merged into production the same day it is **241 passed, 2 deselected**. On 2026-09-26, after `experimental/` was retired, it was **243 passed, 2 deselected** (the two new tests cover the promotion guards); with the batch-driver work added the same day the gate now reads **250 passed, 2 deselected** (re-run 2026-09-26: 90 s, exit 0; `src/cygnus/batch.py` and `tests/test_batch.py` were still uncommitted in the working tree then). There is no longer a separate sandbox suite.
- **Google Drive/Colab access differs by harness (checked 2026-09-26; supersedes the 2026-09-25 note that no rclone remote existed).** The workstation's Claude Code harness has one rclone remote, **`gdrive:`** (`scope = drive.file`), which sees `Cygnus/data/tier1/` and everything rclone wrote, but **not** the `Cygnus/colab_runs/` folders Colab's `drive.mount()` wrote (exit 3, directory not found). Another harness on the same account has a `cygnus:` remote whose token did read them. Remote names are therefore local: every stored location is now recorded, with links and per-harness visibility, in `storage/locations.jsonl` (`python -m cygnus.storage`, `docs/STORAGE.md`). The repo still holds manifests only; `docs/tier1_pack/` describes the pack under `Cygnus/data/tier1/` (149 files, checked with `rclone size` 2026-09-26).
- The archive proxy is intermittent (SIMBAD failed for rank 1–20 targets earlier on 2026-09-25, then NASA Exoplanet Archive and TESS TOI failed for `toi-6667-01`). The ten records affected were re-run the same day (see above); any new cross-match left `inconclusive` by an errored service must be re-run before it is treated as complete.
- **Audit-fix pass 2026-09-25 (agent):** ledger run #338 closed as `aborted`; the orphan untracked `campaigns/toi-7176-01/` moved to scratch; `fetch_products` now records no-data as a `not_tested` exclusion instead of a red `RuntimeError`; `cygnus.campaign check` validates step order (`k_mad: calibrated` needs `calibrate_screen`); `sources.module_for_step` has an explicit step→register alias map so executed steps no longer render "status unknown"; the stale early `mast` manifest is listed **restricted** (its 12 zero-SPOC probes were transient; GJ 1214's zero is real) and a restricted-manifest template crash was fixed; pack retirement now fails closed (no zero-row "ok", no hashless-row deletion, path containment) and the ledger refuses measurements with a missing run and re-closing a closed run; `tests/test_tap.py` added; tier-1 config hash no longer includes `generated_utc`; CI is self-reporting (`-rf --tb=line`, JUnit artifact, `if: always()` builds) with a weekly `network-smoke` lane. **Named and reproduced 2026-09-26 (agent).** On a pristine Linux checkout of the pinned commit (`7f15571`, Colab CPU runtime, Python 3.13.15) the gate ran **587 passed, 2 skipped, 1 failed in 45.9 s**: `tests/test_ingest_pack.py::test_ledger_products_csv_export` staged its file under `pack_dir("ned")` but registered the service as `"NED"`, and `PackBuilder.register` derives `dest_rel = path.relative_to(pack_dir(service))` (`src/cygnus/ingest/pack.py:238`), which raises ValueError on a case-sensitive filesystem and passed only on Windows. The one-line literal fix (stage under `pack_dir("NED")`) was verified on Linux inside the pinned clone (1 passed; the clone was then restored to the pin) and is applied in this worktree (uncommitted). Net local gate after the fix and the new `tests/test_colab.py`: **605 passed, 37 deselected, exit 0** (2026-09-26, 179 s). Committing the test fix is the remaining step to a green `ci` run — see `docs/TEST_ARCHITECTURE_PLAN_DRAFT.md`.**
- **`experimental/` retired 2026-09-26 (agent).** The multi-archive safe copy had been merged into `src/cygnus/multi/` and `tests/` on 2026-09-25 but was still committed (38 tracked files). Every one of its 36 tests had a counterpart in `tests/` (the only differences were import paths, one fixture target line and the NSS note wording "known Gaia NSS companion"), so none needed porting. Its `targets.py`, `priorart.py` and `vet.py` duplicated the production `cygnus.targets`, `cygnus.priorart` and `cygnus.campaign.vet`. Its other modules held **older** versions of code that production has since fixed; the one real difference was that `systematics.py` divided z by the red-noise inflation, which production dropped on purpose (it counts red noise twice). Its `campaigns/toi-6666-01-nss/REPORT.md` was **mislabelled**: the title said "TOI-2666.01", but the TIC ID 256429408 and coordinates were TOI-6666.01's. It was superseded by `campaigns/toi-6666-01-nss/`. The whole tree was archived first, including the untracked sandbox ledger `experimental/state/ledger.sqlite` and `__pycache__`: 72 files, 1,083,460 bytes, per-file SHA-256 identical to the source. The archive is at `D:/AO_Artifacts/cygnus_scratch/retired_2026-09-26/experimental/`; it is nonsynced scratch and exists only on the original workstation. The directory was then removed from git and disk. `python -m cygnus.multi.promote` no longer defaults to a sandbox inside the package, and its worktree default no longer resolves to `src/cygnus/`. It now requires `--root` or `CYGNUS_MULTI_ROOT`, defaults the worktree to `cygnus.config.WORKTREE` and refuses a root equal to the worktree.
- **Target-position epoch corrected 2026-09-26** (audit queries 2026-09-25 UTC, `reports/position-epoch-audit-01/`). The `ra_deg`/`dec_deg` copied from the NASA Exoplanet Archive TOI table were labelled `epoch: J2000.0` in every known-object spec. Against Gaia DR3 positions propagated to each epoch, and direct Gaia DR2/DR1 matches, 77 of 78 targets are Gaia DR2 positions at **J2015.5**; one (TOI-6663.01) is a Gaia DR1 position at **J2015.0** (0.45 mas, independently re-queried); none is at J2000.0. No step propagated positions, so no computed result used the wrong label. Against epoch-2000 catalogues (SIMBAD, VSX) the offset is 15.5 yr × PM, at most 5.9″ (TOI-6653.01), inside the 30″ prior-art radii. The specs, both scaffolds (TOI-table sources emit `J2015.5`; pscomppars and manual positions emit `unverified`) and both runner fallbacks are fixed. The TOI-2666.01/TOI-6666.01 NSS reports now attribute their 1.05″/0.57″ SIMBAD offsets to proper motion, and the TOI-2666.01 report now treats its G 13.70 neighbour at 1.33″ (no parallax) as a same-pixel blend that is disfavoured, not excluded: its dilution cap in G is ~0.34 %, versus ≥ 1.2 % events. Not audited: NAME_RESOLUTIONS (Sesame), pscomppars and manual positions.
- **Batch driver and MAST hang fix 2026-09-26.** On 2026-09-26 MAST's product-list service (`Mast.Caom.Products`, astroquery `get_product_list`) hung for minutes per request (curl: no response in 120 s) while the observation query answered in ~2 s, so a single campaign stalled at `fetch_products` for 15 min. Both runners now take each SPOC 120-s light curve from the observation's `dataURL` (one query, no product-list call) with a 120-s MAST timeout. The product lists for all 76 known-object campaigns are identical to the committed ones (0 differences, checked 2026-09-26), and a regression test forbids the product-list call. New `python -m cygnus.batch` (`queue` / `claim` / `run` / `status`, `docs/AGENT_RUNBOOK.md` *Batches*) runs many campaigns unattended: one lock per ledger, per-campaign subprocess and timeout, orphan-run repair before retry, retry with backoff for transient errors only, a resumable journal and an escalation-first `SUMMARY.md` under `state/batches/`. It never reviews, publishes or commits.
- **Catalogue cones ordered 2026-09-26.** Gaia, SIMBAD, NED and IRSA cone queries had no `ORDER BY`, so an identical re-run returned the same rows in a different order. That changed a product checksum (the TOI-2666.01/TOI-6666.01 Gaia cones during the record regeneration), and a `TOP` cap could keep arbitrary rows. Each now selects its distance from the target (`sep_deg`; `sep_arcsec` for IRSA, whose `DISTANCE` returns arcsec) and orders by it with an ID tiebreak (services reject the function inside `ORDER BY`). Three repeated live queries give identical results, and a test pins the ordering. Record regeneration 2026-09-26: all 78 known-object and NSS campaigns were re-run through `cygnus.batch` (78/78 finished, 0 failed, 3 jobs). Against the previous commit, a leaf-by-leaf diff shows only bookkeeping, the epoch label and position source, the renamed `parametric_z` (values identical in all 1,150 events), the interpretation text, prior-art strings re-ordered but with identical match sets, and the NSS proper-motion check. No result value changed.
- **Red-noise field renamed 2026-09-26.** `cygnus.multi.systematics` used to emit `parametric_z_rednoise_inflated`, which always equalled `robust_z` because the inflation is reported but not applied (`rednoise_inflation_applied: false`). It now emits `parametric_z`, and the check text reads "z vs random-epoch null". The committed `campaigns/**` runner JSON and records still carry the old key until the regeneration pass rewrites them.
- Two orphan ledger runs left `open` by the 2026-09-25 supersede pass: #1211 `cygnus_multi:toi-2435-01:calibrate_screen` and #1302 `cygnus_multi:toi-2263-01:fetch_products`. Completed runs #1442 and #1393 respectively superseded them, and no committed record references the open runs. **Resolved 2026-09-26:** both closed as `aborted` (`Ledger.close_run`) with a note naming the superseding run.

- **Suite expansion 2026-09-26 (agent).** `docs/SUITE_EXPANSION.md` was implemented end to end (§7 there). Bugs found and fixed: `test_multi_cli.py` inspected `D:\` instead of the repo; the multi runner crashed where the campaign twin records a no-product exclusion (now identical, guarded by `tests/test_twin_equivalence.py`; an outage still fails); alias coverage assumed a 120-s cadence in both twins; SkyView sent no position; SkyBoT/context fetches wrote empty files; MPC counted characters; the MAST fast filter matched directories; `tap_csv` accepted error bodies; three `load_spec` ordering holes; ESO spectra typed as light curves; centroid series were dead code; the vet difference-image check passed large-but-insignificant offsets; Legacy Survey cutouts sent a float size (HTTP 500); Gaia `parallax_error` was not fetched. New vendored data: Mamajek mean-dwarf table v2022.04.16 (`src/cygnus/multi/data/`). Limits: ESO TAP timed out on every live probe that day (recorded unavailable); SkyBoT returns server-side SIGBUS for some epochs (recorded unanswered). Committed records predate the new steps (open decision 6).

- **Colab batch pilot completed 2026-09-26 (agent).** Route: Google's `colab-mcp` MCP bridge (DSH stdio plugin, `uvx git+https://github.com/googlecolab/colab-mcp`) plus Option B `drive.mount()`. Two harness findings fixed on the way: (1) the MCP tool call timed out at exactly the 60 s DSH default at the same instant colab-mcp's own 60 s handshake window closed — the connection the user later accepted dialed into the already-torn-down proxy session, which is why Colab showed "disconnected"; fixed by `toolCallTimeoutMs: 180000` on the `mcp-colab` entry in the DSH profile's `cordis.patch.yml` (hot-reloads in place, ~13 s). (2) The handoff's feared `drive.file` scope mismatch **did not reproduce**: a mount-written sentinel under `Cygnus/colab_runs/` was listed and read back by the workstation's `cygnus:` token minutes later (SHA-256 matched). Pilot run: pinned `7f15571`, CPU-only runtime (Python 3.13.15, 2 vCPU, 13 GB RAM), batch `equiv-colab-2026-09-26` with the three acceptance campaigns — **3/3 finished, 0 failed, 37 s wall at `--jobs 3`** (10.1 / 33.3 / 36.5 s per campaign). Equivalence against baselines extracted with `git archive` *before* the run: 21,846 JSON leaves identical; **0 science and 0 record/check-state differences** (11 checks per campaign, same state); the only numeric deltas were floating-point round-off, largest **7.242e-10** relative (identical code, different BLAS/build), plus 85 bookkeeping leaves and 12 prior-art strings differing only in an embedded timestamp. Shipped to `cygnus:Cygnus/colab_runs/equiv-colab-2026-09-26/`: companion Colab ledger, batch journal + per-campaign logs, `equivalence.txt`, `archives_check.txt`, `provenance.json`, `MANIFEST.sha256` — 11 files downloaded to local scratch and SHA-256-verified 11/11; the companion ledger is archived, **never merged**, and campaign outputs were deliberately not copied over committed records (they cite local ledger `run_id`s). Archive health from Google's egress IPs: exit 0, **18 ok / 6 unavailable / 0 error in 153 s**; `earthdata.nasa.gov` returns **403 Forbidden** from Google IPs (reachable from this workstation — an egress difference for any Colab campaign touching it), ESO TAP read-timed out at 120 s as recorded locally. Deliverables: `notebooks/cygnus_batch_colab.ipynb` (11 cells: CPU check + mount, pinned clone + pytest gate, re-entrant state restore, visibility probe, archives check, batch, equivalence via `cygnus.colab`, manifest upload, opt-in-out-by-default campaign copy) and `src/cygnus/colab.py` with 15 offline tests (`tests/test_colab.py`). **Compute units actually used: not read from Colab's Resources panel — read it and record the observed number before quoting any rate.**
- **Colab pilot re-verified 2026-09-26 (workstation harness).** The 11 files of the local copy (scratch `colab_runs/equiv-colab-2026-09-26/`) match its `MANIFEST.sha256`. The journal (3/3 ok; 10.1/33.3/36.5 s), the Colab ledger (18 runs, 06:25:24–06:25:53 UTC), the archive check (18 ok / 6 unavailable / 0 error; Earthdata 403, ESO timeout) and the known-signal states (passed / failed / not_tested, equal to the committed records) agree with the report. Two corrections: (1) **Colab's pytest gate failed**: `reports/step2_install_pytest.log` ends `1 failed, 587 passed` (`test_ingest_pack.py::test_ledger_products_csv_export`, a test-only path-case bug on Linux, fixed in the test and probably also why the GitHub `ci` lane fails), and the batch was run past the notebook's gate. (2) The `drive.mount()` visibility result holds only for the other harness's token (entry above). The equivalence numbers themselves could not be recomputed here, because campaign outputs were deliberately not shipped.
- **Fleet re-run with the new checks, 2026-09-26 (open decision 6 resolved by the user: re-run all).** All 76 known-object campaigns and both NSS campaigns re-run (`state/batches/suite2-2026-09-26`, 78/78 ok; one transient Windows file lock retried). A leaf-by-leaf comparison with the previous commit shows no science change: only timestamps, and the NSS Gaia cones' new `parallax_error` column. New-check totals over the 76: identification 76 passed; priors 60 passed / 16 inconclusive; blend census 26 passed / 50 inconclusive; per-event census 46 failed / 9 passed / 2 inconclusive / 19 not tested (no persistent events); moving objects 32 passed / 23 inconclusive / 21 not tested; VSX 75 passed / **1 failed (TOI-2065.01: a catalogued EA binary, P = 71.96 d, 0.9″; SIMBAD SB*)**; SIMBAD 72 passed / 4 inconclusive; independent repetition and ZTF not tested everywhere (no products fetched). The review also found and fixed a moving-object bug: SkyBoT distances carry units and were read as missing, and positions were geocentric, so any bright object within 10′ failed the check. It now queries TESS-centred positions (observatory C57) and counts only objects within 63″ plus their motion. Each reviewed REPORT.md has a factual addendum; the lead vetting log has the per-lead reading. Known hotspot: `period_aliases` takes 6–18 min on TOI-6695.01 (29 repeat events), already before the suite expansion.

- **Independent archive sources wired in, 2026-09-26 (agent).** New multi step `fetch_independent` (after `fetch_products`, before `alias_cross_instrument`; enforced by the runner's ordering rule and emitted by the scaffold) fetches **ZTF light curves via the `irsa` adapter** (cone 3.0″ default, per-oid position cut `max_offset_arcsec` 2.0″ against the per-point ra/dec median) and **Kepler/K2 long-cadence products via the `mast` adapter** (discovered by position through `Mast.Caom.Filtered.Position`; comma-list collections; header RA_OBJ/DEC_OBJ guard). Query bookkeeping is honest: per-product `not_fetched` rows, `fetch_errors`, and a query is `failed` only when nothing it found was delivered — an outage can no longer read as a successful empty answer. A foreign Kepler/K2 product is recorded but contributes **no** verdict (a flat neighbour would otherwise falsely exclude an alias). Measured live (scratch `fetch_independent_probe/`): MAST Kepler-10 LLC discovery 16.6 s / 15 products / 5.94 MB; IRSA ZTF for TOI-6047.01 at 3″: 1,721 catflags-0 points, 5 oids, 1.37 MB, 29.5 s (Kepler-10 itself: 0 ZTF rows, radius-independent — saturated). The known-object e2e tests are fully offline via mast/irsa fakes; the full offline gate after this work reads **639 passed, 37 deselected**, and the L2 replay lane (4 campaigns) reproduces identically, so the `fetch_products` helper extraction is behaviour-preserving. Old specs without the step stay valid.

- **Colab stress batch, chunk 1 of `colab-p01` (100 of 1,000 periodic PC/APC TOIs), 2026-09-26.** Run on Colab CPU High-RAM (8 vCPU / 51 GB, Python 3.13.15) at `--jobs 7`, pinned to `2b65d44`; the notebook was cut to 100 targets by interrupt (7 runs aborted at 10:25:47Z were all re-run and completed; 84 + 16 resume = 100). Checkpoint shipped to `cygnus:Cygnus/colab_runs/colab-p01/` (3,330 objects, 982.2 MiB) and **verified intact**: 3,327/3,327 manifest hashes match, all 100 run specs byte-match the committed specs, and all 387 product SHA-256s inside the records match the Drive files. Companion Colab ledger (option (a), **never merged**): `ledger.sqlite` SHA-256 `1eecab66bd9e7b8e6d73e064239509f8ea54f098eaf1c7a7cac67a21ab08b941` (verified copy in scratch `verify_colab_p01/`). The batch is registered in `storage/locations.jsonl`. **Outcomes: 57 lead ("Unverified lead") + 43 pipeline_check, 0 failed**; catalogue cross-match passed 100/100; guards: VSX collision failed 4 (toi-6806, 3157, 3241, 621), SIMBAD object-class failed 1 (toi-630, EB* at 0.1″), moving objects failed 1 (toi-2782); positive control failed 5 (toi-340, 1149, 1986, 3157, 6568); 8 no-data campaigns (1949, 2183, 5670, 6137, 7018, 7420, 7780, 7814). The 100 records, reports and search logs were imported into `campaigns/` (3 files each; runner products stay on Drive), all pass `cygnus.skyrecord.validate`, and the local board regenerated from Drive matches byte-for-byte apart from the timestamp. **Lead retest: 57/57 clean** — 1,243 raw recomputes (1,129 event depths + 114 excluded-alias re-derivations) from the raw Drive products reproduce every headline value within 0.2 %. Two code findings for the maintainers (not patched yet): (1) the *"Pointing and quality census per event"* check fails at fleet scale (85/100 here; 46/76 in the earlier fleet re-run) — over-strict or buggy, not 85 science signals; (2) `cygnus.batch` triage does not escalate a moving-objects failure, so toi-2782-01 was missing from `SUMMARY.md`. Remaining: targets 101–1000 are queued but not yet run (user paused after chunk 1); compute units for the run not yet read from Colab's Resources panel (user-only; wall time 3,088.7 s).

- **Site consistency, queue-repeat audit and register fix, 2026-09-26 (agent).** The v1 site sections (`/campaigns/`, `/candidates/`, `/log/`) render only curated `publish/collections` manifests; only 2 campaigns had ever been listed, so nothing new appeared there when the explorer did (predates this work). Two collections now publish all 178 completed campaigns with spec/report/search-log pages (563 pages total); lead dossiers are deliberately withheld until vetting (open decision 8). **Queue-repeat audit (user request):** the 1,000-line `batch_colab-p01.txt` queue has no duplicate ids, no TOI name in both pools, and no pending target whose host was already run in the completed 100 — the queuer produced no repeats. What it does contain are legitimate same-host/different-planet pairs: 3 hosts with two periodic candidates each in the pending 900 (TIC 127505658 → toi-5564-01/02; 179230828 → toi-5000-01/02; 427320001 → toi-2112-01/02), TOI-790.01 (pending) sharing its host with the already-run TOI-790.03, and the already-run pair TOI-6650.01/TOI-6650.04. **Module register:** the site's `ANALYSIS_STACK.md` Layer-2 register lacked every `multi/` row, so the newer campaign steps rendered "status unknown" on the public pages and `test_every_published_campaign_step_resolves_to_a_register_row` (which checks the real worktree) would fail; three `multi/` rows were added and `STEP_MODULE_ALIASES` extended (10 steps), publish tests 55/55 green. **Colab runs standardized:** new dataset collection `colab-p01-runs` publishes the run journal (1,107 runs) as a sanitized CSV through the site's public-run allowlist, the 387 MAST products as the standard manifest view (linked, never re-hosted; held bytes reconcile to 760,057,920), the batch summary, and a curated run-record page — the companion ledger stays unmerged and its ids are labelled as such.

## Environment notes

- Windows workstation originally; `.gitattributes` forces LF line endings.
- Private storage and scratch paths from `AGENTS.md` exist only on the original workstation. A cloud or other machine has no Drive access and no ledger; the test suite and both site builds still run (`python -m pytest -q`, `python -m cygnus.publish build`, `python design-system/mockups/build_explorer.py`).
- Never commit credentials, `rclone.conf`, FITS products, `state/` or `.env` (`.gitignore` covers them).
