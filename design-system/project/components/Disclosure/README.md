# Disclosure

A `details.disclose` block for secondary bulk: verbatim queries, full specification text.

- Summary states what is inside and how many ("Queries and endpoints, verbatim (28)"). Marker is a mono + / −.
- Never hide caveats, limitations or negative results in a disclosure; those stay visible.
