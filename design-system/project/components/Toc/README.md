# Toc

"On this page" list for long documents: h2 and h3 anchors on a left hairline, sticky beside the text ≥ 1000px.

- Built from the rendered headings; h3 entries indent and drop to `fs-xs`.
- Pair with `.doc` layout (230px / fluid). Headings get a hover `#` anchor link.
