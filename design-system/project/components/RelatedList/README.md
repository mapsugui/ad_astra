# RelatedList

Cross-links from a page to other published material: a mono type label over each title, deduplicated, never linking to itself.

- Consumer provides typed references (`collection:`, `document:`, `campaign:`, `candidate:`); unpublished references are dropped at build, not rendered as dead links.
