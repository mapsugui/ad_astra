"""Campaign runner: executes declarative campaign specs (``campaigns/<id>.yaml``) as ledgered steps.

``python -m cygnus.campaign run campaigns/<id>.yaml`` — see ``docs/CAMPAIGNS.md``.
Needs the ``campaign`` extras (pyyaml, numpy, scipy, astropy, requests).
"""
